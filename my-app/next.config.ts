import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  devIndicators: false,
};

export default nextConfig;
  git config --global user.email "chandulokesh97@gmail.com"
  git config --global user.name "LOKESH531"